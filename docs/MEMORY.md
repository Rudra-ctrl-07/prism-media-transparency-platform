- [x] Phase 9: Enhanced bias visualization alternatives (BiasVisualization now supports radar, bar, and pie charts with a toggle)
- [x] Phase 10: Implemented bias comparison functionality (BiasComparison component that compares multiple articles' bias profiles via a multi-radar chart)
- [x] Phase 11: Added 3D globe visualization alternative to 2D map view (GlobeView component with react-globe.gl integration)
- [x] Phase 12: Product-completion pass — zero-config demo data mode, live dashboard shell (stats bar, LIVE indicator with 60s auto-refresh, alerts ticker), wired every tab with shared data (Bias Compare, Sources, Debate, Business), per-article verification analysis (live endpoint with demo fallback), real JSON/CSV export, feed filters, and unit tests for the data layer (7 total)
- [x] Phase 13: Persisted user state via localStorage — saved articles (bookmark buttons + "Saved" feed filter, durable snapshots), dismissible alerts that stay dismissed, user-defined watch alerts (keyword + source with live match counts), and the last active tab; all verified to survive reloads
- [x] Phase 14: Comparison workflow — Compare checkboxes on feed cards with a floating selection bar (Go → Bias Compare), and a BiasComparePanel with searchable picker, removable chips, and a radar charting exactly the selected articles; capped at 3 for readability, selection persisted to localStorage
- [x] Phase 15: Story Tracker view — storyClustering service (stemmed-title keyword overlap + union-find) groups articles into stories; the new Story Tracker tab shows corroboration counts across distinct sources, shared keyword chips, credibility ranges, most-corroborated/recent sorting, per-article verification modal, and a collapsible solo-coverage section; 6 demo articles added so the demo dataset contains 4 multi-source stories; 7 new unit tests (14 total)
- [x] Phase 16: Full product overhaul — real data pipeline + dark pro theme + dashboard layout. Backend: `articleStore` (Firestore when `FIREBASE_SERVICE_ACCOUNT` set, in-memory otherwise), parallel RSS ingestion from 7 live feeds on boot + 15-min refresh, deterministic geocoder, store-based `/api/articles` routes, auth pass-through in zero-config mode, deep-verify without Pro when Firebase is unconfigured. Frontend: `fetchSources` derives live roster from served articles; 20s re-probe flips demo→LIVE automatically. Design: dark token palette in `tailwind.config.cjs` + legacy-color and dark-Leaflet overrides in `index.css`; new `DashboardView` (3-pane: feed rail / credibility map / alerts+stories rail) replaces the tab bar as the home screen, with tools behind a header Tools menu and breadcrumb navigation; landing page rebuilt with a live-headlines hero; fixed `stroke-width` React warnings in BiasVisualization/BiasComparison; Gauge/AppShell/Pricing/Login/Upgrade recolored for dark
- [x] Phase 17: World-Monitor-class intel suite — (1) `IntelMapView`: 2D/3D global situation map with time-range filters (1h–All-time) and 41 toggleable layers across 5 categories (security/nuclear/transport/hazards/socio) fed by `data/intelData.ts` curated real-world datasets + LIVE keyless NWS weather + USGS quake fetchers (`services/intelLayers.ts`); (2) `IntelBriefView`: World Brief synthesized from live feed + layer activity, Threat Timeline (severity/country/date), Strategic Posture (air/naval per theater), AI Forecasts (probability models), Country Instability Index (model base + live-activity delta), Strategic Risk Overview (`services/intelDerived.ts`); (3) `IntelMarketsView`: live broadcasts + webcams, Market Stress monitor (feed-sentiment derived), Supply Chain / Energy / Climate / Big Mac panels; wired into the Tools menu with a dashboard Intel shortcut; 13 new unit tests (27 total)
- [x] Phase 18: OSINT command-center additions — backend `/api/live/adsb` proxies the OpenSky Network (server-side, 5-min cache, even-sampled to ≤800 states, `compactAdsbStates` + `isLikelyMilitary` heuristic in `routes/live.ts`); new live military-aircraft (ADS-B) + AI data-centers layers (43 total); IntelMapView renders per-point colors, plane glyphs, auto-refresh + ADS-B status; Markets gained Metals & Materials (live gold-api.com quotes with reference fallback), China Logistics Corridors, and a News-to-Market impact model (`buildMarketImpact` scoring articles into directional calls); World Brief sections cite sources; CNN broadcast + Kyiv webcam added; NWS fetcher fixed (User-Agent header + no bogus `limit` param); 8 API tests + 8 frontend tests (50 total)
- [x] Phase 19: Neo-brutalist arcade restyle (user request) — full-app re-theme from dark-pro to electric-yellow canvas (#ffe600) with black ink, thick black outlines (2–3px), hard offset shadows (`shadow-brutal*`), and blocky Archivo Black / Space Grotesk typography. `arcade-*` accent tokens (green #22c55e, pink #ff4fd8, blue #4fc3f7) added to `tailwind.config.cjs`; `index.css` updated (light Leaflet tiles, brutal popups/zoom controls, black scrollbars, yellow selection). Dashboard header LIVE badge is a black pill with pulsing red dot; header buttons get hard-shadow hover-press effects; `DashboardView` 3-pane rails + overlay cards, `MapView` legend, `AppShell` marketing nav, `Landing` hero (skull badge, stat counters, LIVE tag, color-coded feature-card grid) and `Pricing` all restyled; intel charts recolored to the arcade palette. Verified: TSC clean, 42 frontend + 8 API tests, build OK, 15/15 headless checks (yellow body on every view, 3-pane dashboard, all 9 tool views render, map tiles + globe canvas, zero console errors)